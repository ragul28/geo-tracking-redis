# Geo Tracking

Geo tracking using redis & golang.

### Build & Run

* Build go project & docker 
    ```sh
    make build
    ```

* Run the project using redis in docker
    ```sh
    make redis-docker
    make run
    ```
